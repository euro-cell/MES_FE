/* ===========================
   메뉴 접근 관리 페이지 전체
=========================== */
.permissionPage {
  padding: 24px;
  background: #f8fafc;
  min-height: 100vh;
  overflow-y: auto;
  box-sizing: border-box;
}

/* ===========================
   각 섹션 (직급별 / 사용자별)
=========================== */
.permissionSection {
  margin-bottom: 10px;
  padding: 20px;
  background: #ffffff;
  border-radius: 10px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
  position: relative;
}

.sectionHeader {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15px;
  border-bottom: 2px solid #e0e0e0;
  padding-bottom: 6px;
}

.sectionHeader h2 {
  color: #1b263b;
  font-size: 20px;
  font-weight: 600;
  margin: 0;
}

.sectionHeaderLeft {
  display: flex;
  align-items: center;
  gap: 16px;
}

.saveButton {
  padding: 8px 16px;
  border: none;
  background: #1b263b;
  color: white;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 500;
  font-size: 14px;
  transition: background-color 0.2s;
}

.saveButton:hover {
  background: #33415c;
}

/* ===========================
   권한 테이블
=========================== */
.permissionTable {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
  font-size: 14px;
  background: #fff;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 1px 4px rgba(0, 0, 0, 0.05);
}

.permissionTable th,
.permissionTable td {
  border: 1px solid #ddd;
  text-align: center;
  padding: 8px;
  vertical-align: middle;
}

.permissionTable th {
  background: #e9ecef;
  font-weight: 600;
  color: #1b263b;
}

.permissionTable td input[type='checkbox'] {
  transform: scale(1.1);
  cursor: pointer;
}

.permissionTable input[type='checkbox'] {
  transform: scale(1.1);
  cursor: pointer;
}

/* ===========================
   하단 저장 버튼 영역
=========================== */

/* ===========================
   사용자 선택
=========================== */
.userSelector {
  display: flex;
  align-items: center;
  gap: 10px;
}

.userSelector label {
  font-weight: 600;
  color: #1b263b;
}

.userSelector select {
  padding: 6px 10px;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 14px;
  outline: none;
  min-width: 160px;
}

.userSelector select:focus {
  border-color: #1b263b;
}

/* ===========================
   메뉴 depth 구분
=========================== */
.menuDepth1 td {
  background: #dce3ee !important;
}

.menuDepth1 td:first-child {
  font-weight: 700;
  color: #1b263b;
  text-align: left;
  padding-left: 12px;
  border-left: 4px solid #1b263b;
}

.menuDepth2 td {
  background: #f4f6f9 !important;
}

.menuDepth2 td:first-child {
  font-weight: 500;
  color: #333;
  text-align: left;
  padding-left: 24px;
  border-left: 4px solid #8fa3c0;
}

.menuDepth3 td {
  background: #fafbfc !important;
}

.menuDepth3 td:first-child {
  font-weight: 400;
  color: #666;
  text-align: left;
  padding-left: 40px;
  border-left: 4px solid #c8d4e3;
}

/* ===========================
   반응형 대응
=========================== */
@media (max-width: 1024px) {
  .permissionPage {
    padding: 12px;
  }

  .permissionTable {
    font-size: 12px;
  }

  .actions button {
    width: 100%;
  }
}

@media (max-width: 768px) {
  .permissionSection {
    padding: 15px;
  }

  .permissionTable th,
  .permissionTable td {
    font-size: 13px;
    padding: 6px;
  }

  .actions {
    position: static;
    text-align: center;
  }
}
